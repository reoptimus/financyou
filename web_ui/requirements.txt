# Fichier conserve pour compatibilite (deploiements Streamlit Cloud, scripts
# existants). Les dependances de l'interface web sont desormais declarees a la
# racine du depot dans requirements.txt et dans l'extra "web" de pyproject.toml.
# Ne rien ajouter ici : ce fichier se contente de rediriger vers la racine.
-r ../requirements.txt
